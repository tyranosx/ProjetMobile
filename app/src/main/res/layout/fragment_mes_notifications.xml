<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#D6D6D6">

    <!-- Titre -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="#FFFFFF">

        <TextView
            android:id="@+id/tvTitre"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cr_neaux_critiques"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_marginBottom="8dp" />
    </LinearLayout>

    <!-- Bannière engagements à venir -->
    <LinearLayout
        android:id="@+id/layoutBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:padding="12dp"
        android:background="@color/light_gray">

        <TextView
            android:id="@+id/tvUpcomingEngagements"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="⚡ Engagements à venir"
            android:textStyle="bold"
            android:textColor="#000000" />

        <Button
            android:id="@+id/btnVoirMesEngagements"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Voir"
            android:textColor="#FFFFFF"
            android:backgroundTint="@color/teal_700"
            android:paddingLeft="16dp"
            android:paddingRight="16dp" />
    </LinearLayout>

    <!-- Liste des créneaux -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerCriticalSlots"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp"
        android:clipToPadding="false"
        android:scrollbars="vertical" />
</LinearLayout>